import { TrackingIdentifier, TrackingRequest } from '@shipengine/connect-carrier-api';
export declare class FakeGetTrackRequestBuilder {
    private readonly request;
    constructor();
    Build: () => TrackingRequest;
    SetIdentifiers: (identifiers: TrackingIdentifier[]) => FakeGetTrackRequestBuilder;
    SetMetadata: (metadata: object) => FakeGetTrackRequestBuilder;
}
