import { Currency, DimensionDetails, Package, WeightDetails } from '@shipengine/connect-carrier-api';
import { LabelMessage } from '@shipengine/connect-carrier-api/src/models/labels/label-message';
import { Customs } from '@shipengine/connect-carrier-api/src/models/customs';
export declare class FakePackageBuilder {
    private readonly package;
    constructor();
    Build: () => Package;
    SetPackageCode: (packageCode: string) => FakePackageBuilder;
    SetWeightDetails: (weightDetails: WeightDetails) => FakePackageBuilder;
    SetLabelMessages: (labelMessage: LabelMessage) => FakePackageBuilder;
    SetCustoms: (customs: Customs) => FakePackageBuilder;
    SetInsuredValue: (insuredValue: Currency) => FakePackageBuilder;
    SetDimensions: (dimensionDetails: DimensionDetails) => FakePackageBuilder;
}
