import { CreateLabelRequest, ShippingService } from '@shipengine/connect-carrier-api';
export declare class ServiceDetails {
    readonly shippingService: ShippingService;
    protected readonly ServiceIdentifier: string;
    /**
     * Class delivery configuration to check if service is selected
     * @param shippingService service definition object
     */
    constructor(shippingService: ShippingService);
    IsServiceSelected(serviceIdentifier: CreateLabelRequest): boolean;
}
