import { ThirdPartyRequest } from '../third-party-request';
/**
 * Modify the logic to return the response as an ArrayBuffer.
 */
export declare class ThirdPartyRequestResponseTypeArrayBuffer extends ThirdPartyRequest {
    thirdPartyRequest: ThirdPartyRequest;
    /**
     * Modify the logic to return the response as an ArrayBuffer.
     * @param thirdPartyRequest base request.
     */
    constructor(thirdPartyRequest: ThirdPartyRequest);
}
