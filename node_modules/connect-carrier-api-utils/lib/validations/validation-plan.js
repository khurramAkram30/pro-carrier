"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ValidationPlan = void 0;
/**
 * ValidationPlan is container for validation rules for specify service.
 */
class ValidationPlan {
    /**
     * @param validationForServices Map of service details and validation rules
     */
    constructor(validationForServices) {
        this.validationForServices = validationForServices;
    }
    /**
     * Add validation rule for a service
     * @param serviceDetails service required to apply the validation rule
     * @param validations validation rules to apply
     */
    addValidation(serviceDetails, validations) {
        const existingValidations = this.validationForServices.get(serviceDetails);
        existingValidations.push(validations);
    }
    /**
     * Get validation rules for an active service based on the service details object and the create label request.
     * @param request
     */
    getValidationRulesForActiveServices(request) {
        let validationRules = undefined;
        this.validationForServices.forEach((value, key) => {
            if (key.IsServiceSelected(request)) {
                validationRules = value;
                return;
            }
        });
        return validationRules;
    }
}
exports.ValidationPlan = ValidationPlan;
//# sourceMappingURL=validation-plan.js.map