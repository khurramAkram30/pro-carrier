import { BaseValidationRule } from '../../base-validation-rule';
import { Result } from '../../../result';
export declare class ValidateParametersBySchema implements BaseValidationRule {
    private readonly validateSchema;
    validate(request: object): Result;
    constructor(validateSchema: object);
}
