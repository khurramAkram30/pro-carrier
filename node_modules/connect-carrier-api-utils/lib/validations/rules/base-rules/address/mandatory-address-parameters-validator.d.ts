import { CreateLabelRequest } from '@shipengine/connect-carrier-api';
import { AddressFields } from './address-fields';
import { AddressType } from './address-type';
import { BaseValidationRule } from '../../base-validation-rule';
import { Result } from '../../../result';
export declare class MandatoryAddressParametersValidator implements BaseValidationRule {
    private readonly addressType;
    private readonly fieldsToValidate;
    constructor(addressType: AddressType, fieldsToValidate: AddressFields[]);
    validate(request: CreateLabelRequest): Result;
}
