import { CreateLabelRequest } from '@shipengine/connect-carrier-api';
import { BaseValidationRule } from '../../base-validation-rule';
import { Result } from '../../../result';
/** @description Basic weight validation */
export declare class ParcelWeightValidator implements BaseValidationRule {
    private readonly maxWeight;
    private readonly minWeight;
    constructor(minWeight: number, maxWeight: number);
    validate(request: CreateLabelRequest): Result;
}
