import { CreateLabelRequest } from '@shipengine/connect-carrier-api';
import { BaseValidationRule } from '../../base-validation-rule';
import { Result } from '../../../result';
export declare class CheckPackagesMaxDimensions implements BaseValidationRule {
    private readonly maxDimensions;
    private readonly dimensionCaptions;
    validate(request: CreateLabelRequest): Result;
    constructor(maxDimensions: number[]);
}
