import { DocumentFormat, LabelLayouts, CreateLabelRequest } from '@shipengine/connect-carrier-api';
import { BaseValidationRule } from '../../base-validation-rule';
import { Result } from '../../../result';
export declare class LabelSizeAndFormatValidator implements BaseValidationRule {
    private readonly supportedLabelLayouts;
    private readonly supportedLabelFormats;
    constructor(supportedLabelLayouts: LabelLayouts[], supportedLabelFormats: DocumentFormat[]);
    validate(request: CreateLabelRequest): Result;
}
