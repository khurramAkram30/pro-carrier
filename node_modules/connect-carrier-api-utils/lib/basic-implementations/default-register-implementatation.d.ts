import { Credentials, RegisterRequest, RegisterResponse } from '@shipengine/connect-carrier-api';
export declare class DefaultRegisterImplementation implements RegisterResponse {
    private readonly request;
    /**
     * Default register implementation
     * @param request entry request, used as source for metadata
     * @param userName if empty then guid will be generated
     * @param password if empty then guid will be generated
     */
    constructor(request: RegisterRequest, userName?: string | undefined, password?: string | undefined);
    credentials: Credentials;
    metadata: object;
}
