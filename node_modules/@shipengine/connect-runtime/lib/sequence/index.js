"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.setSequence = exports.sequence = void 0;
const logger_1 = require("../logger");
let wasSequenceSet = false;
const setSequence = (newSequence) => {
    // We only want to be setting this once per application. It's not an error if it's set again, but it's
    // not how we're intending this to be used so let's just log a warning if it happens.
    if (wasSequenceSet) {
        const message = 'setSequence has been called on an instance more than once. ' +
            'This should not be done and can lead to unexpected behavior.';
        logger_1.logger?.warn(message);
    }
    exports.sequence = newSequence;
    wasSequenceSet = true;
};
exports.setSequence = setSequence;
//# sourceMappingURL=index.js.map