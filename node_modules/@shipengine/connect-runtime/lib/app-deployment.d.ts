/** Type expected by the Connect CLI for deployment information about a package */
export interface AppDeployment {
    /** Id of the package */
    id: string;
    /** Validate the app before deployment */
    validate?: () => string[] | undefined;
    /** Get the countries supported by this app */
    getSupportedCountries?: () => string[];
}
