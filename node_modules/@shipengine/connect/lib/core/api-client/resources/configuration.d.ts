import ShipengineAPIClient from '..';
import { ConfigurationKey } from '../../types/configuration-key';
export default class Configuration {
    private client;
    constructor(apiClient: ShipengineAPIClient);
    list(appId: string): Promise<ConfigurationKey[]>;
    set(appId: string, configurationKey: ConfigurationKey): Promise<ConfigurationKey>;
    unset(appId: string, name: string): Promise<ConfigurationKey>;
}
