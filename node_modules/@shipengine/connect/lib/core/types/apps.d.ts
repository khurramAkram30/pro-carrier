import { AppType, DeploymentType } from '../app-loader';
export type SdkAppTypes = AppType.Carrier | AppType.Freight | AppType.Order | AppType.Fulfillment;
export interface ProductInfo {
    product: 'ShipStation';
    loginUrl: string;
}
export interface ConnectApp {
    id: string;
    name: string;
    type: DeploymentType;
    runtime: string;
    productInfos: ProductInfo[];
}
