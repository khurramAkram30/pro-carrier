import { App } from './app-loader';
declare class InvalidAppError extends Error {
    errors: string[];
    code: string;
    constructor(message: string, errors: string[]);
}
export default function loadAndValidateApp(pathToApp: string): Promise<App>;
export declare function isInvalidAppError(obj: unknown): obj is InvalidAppError;
export {};
