const {
  connect,
  delegateFulfillment,
  getFulfillments,
  cancelFulfillment,
  getInventory,
  getRates,
  getRecentChanges,
} = require('./methods');
const { Metadata } = require('./definitions');

module.exports = {
  connect,
  delegateFulfillment,
  getFulfillments,
  cancelFulfillment,
  getInventory,
  getRates,
  getRecentChanges,
  Metadata,
};
