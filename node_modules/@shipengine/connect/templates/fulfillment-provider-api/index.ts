import { FulfillmentProviderAppDefinition } from '@shipengine/connect-fulfillment-provider-api';
import {
    connect,
    delegateFulfillment,
    getFulfillments,
    cancelFulfillment,
    getInventory,
    getRates,
    getRecentChanges,
} from './methods';
import {
    Metadata
} from './definitions';

export default {
    connect,
    delegateFulfillment,
    getFulfillments,
    cancelFulfillment,
    getInventory,
    getRates,
    getRecentChanges,
    Metadata,
} satisfies FulfillmentProviderAppDefinition;
