import Joi from 'joi';
import { TaxIdentifierTypeSchema } from './tax-identifier-type-schema';

export const TaxIdentifierSchema = Joi.object({
  id: Joi.string(),
  type: TaxIdentifierTypeSchema,
  registration_county: Joi.string().optional(),
  description: Joi.string().optional(),
});
