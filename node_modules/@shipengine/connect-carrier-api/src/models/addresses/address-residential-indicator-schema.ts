import Joi from 'joi';
import { AddressResidentialIndicator } from './address-residential-indicator';

export const AddressResidentialIndicatorSchema = Joi.string().valid(
  ...Object.values(AddressResidentialIndicator),
);
