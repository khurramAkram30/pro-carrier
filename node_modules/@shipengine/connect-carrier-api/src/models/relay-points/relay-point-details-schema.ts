import Joi from 'joi';
import { RelayPointAddressSchema } from './relay-point-address-schema';

export const RelayPointDetailsSchema = Joi.object({
  ship_to: RelayPointAddressSchema.required(),
  ship_from: RelayPointAddressSchema.required(),
});
