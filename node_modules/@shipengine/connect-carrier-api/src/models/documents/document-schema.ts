import { DocumentTypeSchema } from './document-type-schema';
import { DocumentFormatSchema } from '../labels/document-formats-schema';
import Joi from 'joi';

export const DocumentSchema = Joi.object({
  type: Joi.array().required().min(1).items(DocumentTypeSchema),
  data: Joi.string().required(),
  format: DocumentFormatSchema.required(),
});
