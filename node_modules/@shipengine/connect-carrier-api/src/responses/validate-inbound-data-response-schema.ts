import Joi from 'joi';
import { InboundDataContentTypesSchema } from '../models/inbound-content-types-schema';
import { BaseResponseSchema } from './base-response-schema';

export const ValidateInboundDataResponseSchema = BaseResponseSchema.keys({
  content_types: Joi.array().required().items(InboundDataContentTypesSchema),
});
