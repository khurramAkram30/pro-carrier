import Joi from 'joi';
import { VoidResponseSchema } from '../models/labels/void-response-schema';
import { BaseResponseSchema } from './base-response-schema';

export const VoidLabelsResponseSchema = BaseResponseSchema.keys({
  void_responses: Joi.array().required().items(VoidResponseSchema),
});
