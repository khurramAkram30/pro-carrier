import Joi from 'joi';
import { RateSchema } from '../models/rates/rate-schema';
import { BaseResponseSchema } from './base-response-schema';

export const GetRatesResponseSchema = BaseResponseSchema.keys({
  rates: Joi.array().required().items(RateSchema),
});
