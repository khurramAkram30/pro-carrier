import { ServicePointSchema } from '../models/service-points/service-point-schema';
import { BaseResponseSchema } from './base-response-schema';

export const GetServicePointResponseSchema = BaseResponseSchema.keys({
  service_point: ServicePointSchema.required(),
});
