import Joi from 'joi';
import { RelayPointSchema } from '../models/relay-points/relay-point-schema';
import { BaseResponseSchema } from './base-response-schema';

export const GetRelayPointsResponseSchema = BaseResponseSchema.keys({
  relay_points: Joi.array().required().items(RelayPointSchema),
});
