import Joi from 'joi';
import { CredentialsSchema } from '../models/registration/credentials-schema';

export const RegisterResponseSchema = Joi.object({
  credentials: CredentialsSchema.required(),
  metadata: Joi.object().pattern(/.*/, [Joi.any()]).required(),
});
